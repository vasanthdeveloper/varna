/*
 *  Builds a compressed single file out
 *  of a template directory.
 *  Created On 02 April 2021
 */

export default async (): Promise<void> => {
    console.log('build a compressed single file out of a given directory.')
}
